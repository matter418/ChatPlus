{
  "name": "ChatPlus",
  "version": "1.1.0",
  "description": "Chat+ — split chat into WoW-style tabs (All / Local / Global / Whispers) over the game's own chat, with unread flashing, resizable height, font size, and per-channel colours.",
  "packageManager": "yarn@4.9.1",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "node build.mjs"
  },
  "main": "src/ChatPlus.ts",
  "license": "GPL-3.0-or-later",
  "author": "matter",
  "devDependencies": {
    "@ryelite/core": "^0.1.0",
    "esbuild": "^0.25.8",
    "typescript": "^5.8.3"
  }
}
